import { test } from "bun:test";
import { testnetBottle } from "./support/testnet_bottle";
import { sleep } from "bun";


test("bottle", async () => {
  let a = testnetBottle();
  await sleep(60 * 1000);

}, {timeout: 60 * 1000})
