{
  "name": "libra-ts-sdk",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "lint": "eslint *.ts --no-eslintrc && prettier --check *.ts",
    "format": "eslint *.ts --no-eslintrc --fix && prettier --no-config --write *.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.7.7"
  }
}
